io:
  github:
    ilyasdotdev:
      microservice:
        security:
          jwt:
            rsa:
              private-key: classpath:privateKey.pem
              public-key: classpath:publicKey.pem